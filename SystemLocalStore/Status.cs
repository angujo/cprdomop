namespace SystemLocalStore
{
    public enum Status
    {
        STARTED,
        RUNNING,
        PAUSED,
        STOPPED,
        ERROR,
        SHUTDOWN,
        QUEUED,
        ERROR_EXIT,
        COMPLETED,
    }
}
